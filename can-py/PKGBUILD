pkgname=can-py
pkgver=4
pkgrel=1
pkgdesc="meta-package for basic python-apps container setup."
arch=(any)

depends=(
	can-base

	git
	patch
	rsync

	python
	python-pip
	python-setuptools
	python-virtualenv

	python-libnacl
	python-systemd
)

provides=()
conflicts=()
replaces=()

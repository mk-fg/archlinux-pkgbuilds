pkgname=can-tools
pkgver=1
pkgrel=1
pkgdesc="meta-package for less-than-basic container setup."
arch=(any)

depends=(
	can-base

	atop
	busybox
	colordiff
	dash
	fping
	git
	ipset
	iptables
	lz4
	patch
	pv
	rsync
	screen
	xz
	zsh
)

provides=()
conflicts=()
replaces=()

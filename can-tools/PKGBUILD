pkgname=can-tools
pkgver=2
pkgrel=2
pkgdesc="meta-package for less-than-basic container setup."
arch=(any)

depends=(
	can-base

	atop
	busybox
	colordiff
	dash
	fping
	git
	ipset
	iptables
	lz4
	openssh
	patch
	pv
	rsync
	screen
	xz
	zsh
)

provides=()
conflicts=()
replaces=()
